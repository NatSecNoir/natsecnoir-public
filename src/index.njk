---
layout: base.njk
nav: feed
permalink: /
---
<div class="shead"><h2>Latest records</h2><span class="n">{{ records.all.length }} record{% if records.all.length != 1 %}s{% endif %}</span></div>
{% include "datefilter.njk" %}
<div class="feed" data-items>
{% for r in records.all %}{% include "item.njk" %}{% endfor %}
{% if not records.all.length %}<p class="empty">No records yet.</p>{% endif %}
</div>
